box: alpine

build:
  steps:
    - squidfunk/mkdocs-material
    - script:
      code: |
        mkdocs build
deploy:
  steps:
    - squidfunk/mkdocs-material
    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: site

