box:
  id: squidfunk/mkdocs-material
  entrypoint: /bin/bash -c
  ports:
    - "8000"

build:
  steps:
    - script:
      code: |
        mkdocs build

deploy:
  steps:
    #- lukevivier/gh-pages:
    #    token: $GH_TOKEN
    #    basedir: site
    - leipert/git-push:
         gh_oauth: $GH_TOKEN
         gh_pages: true
         basedir: site

